{
  "name": "@moderniptvplayer/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc -p tsconfig.json --noEmit",
    "db:push": "prisma db push",
    "db:push:prod": "cross-env NODE_ENV=production prisma db push",
    "db:generate": "cross-env NODE_ENV=production prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "cross-env NODE_ENV=production prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:studio:prod": "cross-env NODE_ENV=production prisma studio"
  },
  "dependencies": {
    "@moderniptvplayer/env": "workspace:*",
    "@neondatabase/serverless": "^1.0.2",
    "@prisma/adapter-neon": "^7.2.0",
    "@prisma/client": "catalog:",
    "dotenv": "catalog:",
    "ws": "^8.18.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@moderniptvplayer/config": "workspace:*",
    "@types/ws": "^8.18.1",
    "cross-env": "^10.1.0",
    "prisma": "^7.2.0",
    "typescript": "catalog:"
  }
}