import { createPageMetadata } from "@/app/metadata";

export const metadata = createPageMetadata({
	title: "Add Stalker Playlist",
	description: "Connect an IPTV playlist through a Stalker Portal endpoint.",
	path: "/dashboard/addplaylist/stalker",
	noIndex: true,
});

function Page() {
	return <div>Page under construction. Please check back later.</div>;
}

export default Page;
